<form class="ikp-component-form" onSubmit="ikpRemoteSubmit(); return false;" action="{{ path('component_update', { 'id': component.id, 'xhr': '1' }) }}" method="post" {{ form_enctype(edit_form) }}>
    <div class="formtextField">{{ form_widget(edit_form.text) }}</div>
            {{ form_rest(edit_form) }}
    <input class="ikp-btn submit" type="submit" />
</form>